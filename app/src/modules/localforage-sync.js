// /*global localforage*/

// localforage && (function(lf) {

//     lf.synchronize = synchronize;

//     function synchronize(url, callback /*(error)*/ ) {
//         //...
//     }

// })(localforage);
